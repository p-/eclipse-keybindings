{
  "name": "eclipse-keybindings",
  "version": "0.7.0",
  "description": "Eclipse keybindings like `Ctrl-Shift-F`, `Ctrl-Shift-R`, `Ctrl-L`, `Alt-↑`, ... (⌘ on Mac)",
  "repository": "https://github.com/p-/eclipse-keybindings",
  "license": "MIT",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  }
}
